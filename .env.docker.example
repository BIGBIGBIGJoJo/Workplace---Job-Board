# Optional. Add the same Google OAuth web client ID to both values to enable Google login.
GOOGLE_CLIENT_ID=
VITE_GOOGLE_CLIENT_ID=

# Optional. Change these if the default local ports are already in use.
FRONTEND_PORT=3000
BACKEND_PORT=4000
MONGO_PORT=27017
